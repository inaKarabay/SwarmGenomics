Fragen:

An welcher Stelle sollen Repeats entfernt werden? Direkt in Referenz (am Anfang der Pipeline) oder erst spaeter aus Consensus? Wenn zweites dann wie? (Ich habe bis jetzt mit Repeats weitergearbeitet)

Fuer PSMC muss bcftools call mit -c angewendet werden (Consensus generiert mit bcftools consensus aus vcf mit call -mv erstellt funktioniert nicht; -mv ist jedoch eigentlich der neue Standard)

Bcftools call mit -A oder nicht? (Wuerde meiner Meinung nach Sinn machen):
   -A, --keep-alts                 keep all possible alternate alleles at variant sites

Bei vcf mit call -mv generiert werden nur Varianten angezeigt, beim vcf mit bcftools call -c generiert wird der Consensus angezeit. Wie kann man hieran die Varianten (und somit heterozygotie) ablesen?

Vcf mit call-c: warum bei AF1 und AC1 immer 0? Bedeutet dies nicht homozygot?

Wie kann Heterozygotie aus Consensus herausgelesen werden? Bedeuten Kleinbuchstaben heterozygot?

Wie kann man die PSMC Bilder interpretieren? (in Ordner results/VulpesLagopus/PSMC)

Demographic woher? (Groesse, Groesse wenn Kind Eltern verlaesst etc.)











